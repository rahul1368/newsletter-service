services:
  # Web Service - Newsletter API
  - type: web
    name: newsletter-api
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    region: oregon
    healthCheckPath: /health
    envVars:
      # Database connection (from PostgreSQL database)
      - key: DATABASE_URL
        fromDatabase:
          name: newsletter-db
          property: connectionString
      # Redis connection (from Redis service)
      - key: REDIS_HOST
        fromService:
          name: newsletter-redis
          type: redis
          property: host
      - key: REDIS_PORT
        fromService:
          name: newsletter-redis
          type: redis
          property: port
      # Redis connectionString (includes auth if needed)
      - key: REDIS_URL
        fromService:
          name: newsletter-redis
          type: redis
          property: connectionString
      # Email service
      - key: RESEND_API_KEY
        sync: false
      - key: SENDER_EMAIL
        value: onboarding@resend.dev
      # Application
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
    # Build command (handled by Dockerfile)
    # Start command (handled by Dockerfile CMD)
    # Migrations run automatically via docker-entrypoint.sh before app starts

  # Redis Cache Service
  - type: redis
    name: newsletter-redis
    plan: free
    region: oregon
    maxmemoryPolicy: allkeys-lru
    ipAllowList: []  # Allow all IPs (empty array means allow all)

databases:
  # PostgreSQL Database
  - name: newsletter-db
    plan: free
    region: oregon
    databaseName: newsletter_db
    user: newsletter_user
